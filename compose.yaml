services: 

  # admin:
  #   image: adminer
  #   restart: always
  #   ports:
  #     - 8080:8080

  fastapi-db:
    container_name: fastapi-db
    build:
      context: /home/kyoko/code/fastAPI-proj/docker/
      dockerfile: Dockerfile
    image: mariadb:lts-noble
    restart: always
    working_dir: /docker
    ports:
      - 3306:3306
    volumes:
      - /home/kyoko/code/fastAPI-proj/docker/:/docker
    environment:
      MARIADB_ROOT_PASSWORD: party
    stdin_open: true
    tty: true
  